server:
  port: 8761
#spring:
#  application:
#    name: eureka-server      spring.application.name 的优先级比 eureka.instance.appname 高
#  profiles: slave1
eureka:
  instance:
    hostname: slave1
  client:
    register-with-eureka: false
    fetch-registry: false
    #service-url:
      #defalutZone: http://slave2:8762/eureka/

#使用以下配置，为了模拟 主备Eureka服务
#server:
#  port: 8762
#spring:
#  application:
#    name: eureka-server
#  profiles: slave2
#eureka:
#  instance:
#    hostname: slave2
#  client:
#    service-url:
#      defalutZone: http://slave1:8761/eureka/
